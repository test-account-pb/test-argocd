apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-svc"
spec:
  selector:
    app: hello-app
  ports:
    - protocol: TCP
      port: "{{ .Values.appPort }}"
      targetPort: "{{ .Values.containerPort }}"
